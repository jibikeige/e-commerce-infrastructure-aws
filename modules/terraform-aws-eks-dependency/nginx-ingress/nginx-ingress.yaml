controller:
  replicaCount: 2

  service:
    type: LoadBalancer
    annotations:
      service.beta.kubernetes.io/aws-load-balancer-type: "nlb"

  ingressClassResource:
    name: nginx
    enabled: true
    default: true

  nodeSelector:
    kubernetes.io/os: linux