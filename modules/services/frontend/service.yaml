apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: ClusterIP   # Only accessible within the cluster
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80      # The port other pods/services will use to reach this service
      targetPort: 3000 # The port your container is listening on